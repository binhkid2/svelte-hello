<h1>Binh dep trai</h1>
<style>
    :global(h1) {
        color:red;
        animation: zoom 5s infinite;
    }
    @keyframes -global-zoom {
        0%,100% {transform: scale(3);}
        50% {transform: scale(0.5);}
    }
</style>